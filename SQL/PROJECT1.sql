-- CREATE TABLE face_recognition_data (
--     id INT AUTO_INCREMENT PRIMARY KEY,
--     name VARCHAR(255),
--     date DATE,
--     time TIME,
--     CONSTRAINT unique_person_data UNIQUE (name, date)
-- );
SELECT * FROM face_recognition_data;
